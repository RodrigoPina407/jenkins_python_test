pipeline {
    agent { 
        node {
            label 'qa'
            }
      }
    triggers {
        pollSCM '* * * * *'
        
    }
    stages {
        stage('Build') {
            steps {
                echo "Building.."
                sh '''
                cd myapp
                cargo build --release
                '''
            }
        }
        stage('Test') {
            steps {
                echo "Testing.."
                sh '''
                cd myapp-rust
                cargo test
                '''
            }
        }
        stage('Deliver') {
            steps {
                echo 'Deliver....'
                sh '''
                cargo run --release
                '''
            }
        }
    }
}